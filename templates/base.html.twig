<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lobster&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ asset('style.css') }}">
    {# j'insère un block pour la balise meta title #}
    <title>{% block metaTitle %}{% endblock metaTitle %}</title>
</head>
<body>
<header class="f-white">
    <h1>Recettes</h1>
</header>
<main>
    {# j'insère un block pour la balise main #}
    {% block main %}
    {% endblock main %}
</main>
    <footer class="f-white">
        {# j'insère un block pour insérer du texte dans la balise footer #}
        {% block textFooter %}
        {% endblock textFooter %}
    </footer>
</body>
</html>